## Leçons apprises

[2026-04-23] | Le todo utilise des slugs de routes différents des routes réelles codées dans App.tsx | Toujours vérifier App.tsx avant de corriger des URLs dans les pages — les routes réelles sont :
- `/` → Accueil (Index)
- `/club` → Le Club (≠ `/le-club` dans le todo)
- `/collectifs` → Nos Collectifs (≠ `/nos-collectifs`)
- `/inscriptions` → Inscriptions (≠ `/registration`)
- `/partenaires` → Partenaires (≠ `/partners`)
- `/contact` → Contact
- `/evenements` → Événements
- `/matchs` → Matchs
- `/mentions-legales` → Mentions légales

[2026-05-18 → corrigé 2026-05-22] | Ne JAMAIS mettre de fallback URL dans le client Supabase | Le fallback "https://placeholder.supabase.co" fait pointer le client vers un domaine fictif : le await supabase.from(...).select() accroche indéfiniment, aucune requête HTTP n'est émise, pas d'erreur visible. Fix définitif : supabase.ts ne contient que createClient(url, key) sans fallback ni condition. Si les variables ne sont pas là, le client crashe à la création — ce qui est visible et diagnostiquable.

[2026-05-22] | authStore : remplacer supabase.auth.* par fetch natif | Le client supabase-js instancié au niveau module peut être figé avec une mauvaise URL (cache HMR). onAuthStateChange ne se déclenche jamais → isLoading bloqué indéfiniment. Fix définitif : authStore.ts n'importe plus supabase depuis @/lib/supabase — tout passe par fetch natif vers /auth/v1/* et /rest/v1/profiles. La session est gérée manuellement dans localStorage (clé sb-{ref}-auth-token). Pattern identique à usePublicMatches.ts.

[2026-05-22] | Le localStorage Supabase peut bloquer _init() | Un token sb-*-auth-token expiré ou corrompu dans localStorage peut faire rester isLoading: true (onAuthStateChange attend un refresh qui ne revient jamais). Fix : nettoyer les clés sb-* expirées en début de _init() + safety timeout à 2s + reset complet dans signIn(). Zustand create() ne persiste rien — le problème vient uniquement du localStorage du client Supabase JS, pas du store Zustand.

[2026-05-22] | Le client Supabase JS est figé à l'instanciation du module | createClient() est exécuté une fois au chargement du module. Si Vite sert un module caché avec une mauvaise URL (placeholder ou undefined), le client reste figé même après HMR. Le await supabase.from() ne génère aucune requête HTTP, sans erreur visible. Fix définitif : utiliser fetch natif dans les queryFn (env vars lues à chaque appel, pas à l'import). Ne plus dépendre du client @supabase/supabase-js dans les hooks publics.

[2026-05-18 → corrigé 2026-05-22] | Ne PAS utiliser enabled: isSupabaseConfigured dans les hooks publics | enabled: isSupabaseConfigured désactive la query si les env vars Vite ne sont pas injectées (ex: dev server démarré avant .env.local, ou build sans variables). La query est alors silencieusement disabled → zéro requête réseau, zéro message d'erreur. Fix : supprimer enabled, laisser TanStack Query catcher les erreurs réseau. SSG-safety garantie par les placeholders dans supabase.ts (pas de throw à l'import). isSupabaseConfigured doit se baser sur l'URL résolue (supabaseUrl !== placeholder), pas sur import.meta.env brut.

[2026-05-22] | Ne jamais appeler navigate() ou setState pendant le rendu | Appeler navigate() directement dans le corps d'un composant (hors event handler) déclenche "Cannot update a component while rendering a different component". Toujours entourer dans un useEffect avec les bonnes dépendances.

[2026-05-22] | data ?? [] dans queryFn pour éviter null as Match[] | Supabase peut retourner null pour un résultat vide selon le contexte. Toujours retourner (data ?? []) as Match[] pour garantir un tableau. Et dans le rendu, ne jamais tester results?.length === 0 seul — utiliser isError + results.length (quand data est garanti non-null par le hook).

[2026-05-22] | Une seule query + split client-side > deux queries séparées | Pour une page qui affiche deux sections (résultats + à venir) du même dataset, préférer un seul useQuery avec .in("statut", [...]) et filtrer côté client. Réduit les requêtes réseau et simplifie la gestion des états loading/error.

[2026-05-22] | signIn() doit set user et isLoading explicitement, pas seulement via onAuthStateChange | Si onAuthStateChange arrive tard ou échoue silencieusement, le store reste bloqué (isLoading: true, user: null). Fix : dans signIn(), après fetchProfile(), appeler set({ user, isLoading: false }) explicitement. Ajouter aussi un safety timeout de 5s dans _init() qui force isLoading: false si toujours vrai.

[2026-05-22] | useMatches (admin CRUD) : même pattern fetch natif que usePublicMatches | useMatches.ts utilisait supabase.from() → même bug de client figé en HMR. Fix définitif : helper baseHeaders() qui lit VITE_SUPABASE_URL et VITE_SUPABASE_ANON_KEY à chaque appel + lit le token depuis localStorage (sb-{ref}-auth-token). Helpers pgList, pgOne, pgInsert, pgPatch, pgDelete couvrent tout le CRUD. Aucun import depuis @/lib/supabase.

[2026-08-30] | Les numéros de migration réels dépassent ceux mentionnés dans une consigne (ex. demande de créer "013_x.sql" alors que 013 existe déjà) | Toujours lister `supabase/migrations/*` avant de nommer un nouveau fichier de migration — ici 013_evenements_photo2.sql existait déjà, la nouvelle migration collectifs est devenue 014_collectifs.sql.

[2026-08-30] | get_my_role() est utilisé dans 011_audit_logs.sql sans être défini dans aucune migration trackée (ni schema.sql) | La fonction existe donc côté Supabase mais a été créée manuellement (hors migration versionnée) à un moment. Avant de réutiliser une fonction SQL "supposée exister", vérifier qu'elle est bien définie dans les fichiers trackés — sinon le prévenir explicitement à l'utilisateur plutôt que de supposer silencieusement. Pattern de repli si la fonction manque vraiment : `EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role IN (...))` (utilisé dans 004_evenements.sql, 005_roles.sql).

[2026-08-30] | zodResolver + useFieldArray sur un schéma imbriqué (ex. horaires: [{jour,debut,fin}]) : le type inféré de `data` dans onSubmit peut avoir des propriétés optionnelles même si le schema Zod interne ne les déclare pas `.optional()` | Ne pas passer `data.champArray` tel quel à un payload typé strictement (ex. `CollectifInsert`) — mapper explicitly chaque champ avec un fallback (`h.jour ?? ""`) pour satisfaire le typage strict côté hook/API, indépendamment de la validation Zod à l'exécution.

[2026-08-30] | Validation Zod stricte (`.min(1)`) sur des lignes de useFieldArray sans affichage d'erreur par ligne = échec de soumission silencieux pour l'utilisateur | Pour des listes dynamiques (horaires, lieux) où l'admin peut laisser une ligne vide sans s'en rendre compte, préférer une validation permissive côté schema + filtrage des lignes vides à la soumission (onSubmit) plutôt que de bloquer avec un message d'erreur qui n'est pas rendu visuellement sur chaque sous-champ.

[2026-08-30] | La liste des catégories était dupliquée à l'identique dans 4 fichiers (`Resultats.tsx` ALL_CATS, `MatchFormPage.tsx`, `MatchListAdminPage.tsx`, `UsersPage.tsx` CATEGORIES) mais avec un ordre incohérent : Séniors Féminines/Masculins inversés dans ALL_CATS par rapport aux 3 autres | Avant de "réutiliser une liste existante" sur consigne utilisateur, grep toutes les occurrences plutôt que de faire confiance à la première trouvée — elles peuvent diverger silencieusement. Fix : `src/data/categories.ts` créé comme source unique (`CATEGORIES` avec "Baby", `MATCH_CATEGORIES` sans). Seul `Resultats.tsx` a été migré vers cette source (scope de la tâche) ; les 3 fichiers admin gardent leur copie locale — à dédupliquer si une tâche future les touche.

[2026-08-30] | Extension Claude in Chrome refusée par l'utilisateur pour cette session | Ne plus la proposer/relancer dans la session. Pour vérifier une UI sans elle : `tsc --noEmit` + `eslint` + `npm run build` (garantit l'absence d'erreur de compilation/type), mais dire explicitement à l'utilisateur que le rendu visuel réel n'a pas été vérifié et lui indiquer l'URL locale (`npm run dev`) à checker manuellement.

[2026-08-31] | Un schéma de table demandé avec une colonne `NOT NULL` peut ne pas correspondre aux données réelles à migrer (ex. `prenom TEXT NOT NULL` alors que src/data/bureau.ts a des postes vacants sans titulaire) | Toujours relire les données sources AVANT d'écrire le CREATE TABLE d'une migration de CMS — si une contrainte NOT NULL forcerait à inventer une valeur ou ferait échouer l'INSERT, la relâcher (nullable) et documenter l'écart en tête de fichier de migration plutôt que de suivre la consigne à la lettre.

[2026-08-31] | Un champ booléen "dérivé" dans les anciennes données statiques (`featured` dans bureau.ts) peut être redondant avec un autre champ (ici : `featured` coïncidait à 100% avec "avatarUrl renseigné") | Avant de reproduire un tel champ dans un nouveau schéma CMS, vérifier s'il peut être simplement dérivé d'un champ déjà présent (`photo_url != null`) plutôt que stocké séparément — évite une colonne inutile et un état qui peut diverger de la photo réellement présente. Appliqué à `encadrement` : pas de colonne `featured`, l'affichage photo/placeholder dépend uniquement de `photo_url`.

[2026-08-31] | Un multi-select alimenté par une liste canonique (MATCH_CATEGORIES) peut ne pas couvrir des valeurs historiques présentes dans les données réelles (ex. entraîneur catégorie "Baby", ou "-15M/-18M" qui n'existe dans aucune liste canonique) | Si on restreint un multi-select aux seules options canoniques, toute valeur existante hors-liste est invisible dans le formulaire et disparaît silencieusement à la prochaine sauvegarde. Fix : construire les options affichées comme l'union de la liste canonique ET des valeurs déjà sélectionnées sur la fiche en cours d'édition (`Array.from(new Set([...MATCH_CATEGORIES, ...valeursActuelles]))`). Correction ultérieure (2026-09-01) : `EncadrementPage.tsx` utilisait `MATCH_CATEGORIES` (qui exclut "Baby Hand" par design, les baby hand ne jouant pas de matchs) — mauvais choix pour ce contexte précis, il fallait `CATEGORIES` (liste complète) puisqu'un entraîneur peut très bien encadrer le Baby Hand.

[2026-09-01] | Pas d'accès direct pour interroger la base Supabase en direct depuis cet environnement Claude Code (le connecteur MCP `claude.ai Supabase` nécessite une authentification via `/mcp`, qui ne se déclenche pas depuis ce contexte) | Avant d'écrire des `UPDATE` de migration sur des données de production dont les valeurs réelles ne sont pas connues avec certitude (ex. `matches.categorie` rempli au fil des sessions admin), ne pas deviner : soit demander à l'utilisateur de lancer un `SELECT DISTINCT` diagnostique et de coller le résultat avant d'écrire le moindre `UPDATE`, soit — si l'utilisateur préfère avancer tout de suite — concevoir la migration en exact-match uniquement (aucune valeur inconnue n'est touchée) avec un `RAISE NOTICE` diagnostique intégré. Dans ce projet, l'utilisateur a choisi la première option : la migration concernée est livrée en 2 étapes (diagnostic seul, puis UPDATE dans un tour suivant).

[2026-09-01] | Restreindre le DELETE définitif à un rôle plus restrictif que SELECT/INSERT/UPDATE (ex. super_admin seul, alors que président a accès au reste) | PostgreSQL ne permet pas de retirer sélectivement une commande d'une policy `FOR ALL` — toutes les policies applicables à une commande donnée sont combinées en OR, donc ajouter une policy `FOR DELETE` plus restrictive ne retire rien à une policy `FOR ALL` préexistante plus permissive. Il faut scinder : `FOR SELECT`/`FOR INSERT`/`FOR UPDATE` avec les rôles larges, et une policy `FOR DELETE` séparée avec le rôle restreint, en supprimant l'ancienne `FOR ALL`. Appliqué dans `019_soft_delete.sql` pour encadrement/collectifs/evenements/tarifs (suppression définitive réservée à super_admin, restauration et édition restent ouvertes à président).

[2026-09-01] | `EvenementsPage.tsx` n'a jamais eu de hook TanStack Query dédié (contrairement à collectifs/encadrement/tarifs) — il gère son fetch en `useState`/`useCallback`/`useEffect` locaux | Ne pas le refactoriser vers TanStack Query juste pour "harmoniser" à l'occasion d'une tâche différente (ici : ajout de la corbeille) — hors scope, risque de régression sur un fichier qui fonctionne. Le pattern soft delete/corbeille a été répliqué en fonctions locales (`loadTrash`, `handleRestore`, `handleHardDelete`) cohérentes avec son architecture existante plutôt que d'imposer le pattern des 3 autres pages.

[2026-09-01] | Un composant de corbeille (liste des éléments supprimés + restaurer + supprimer définitivement) est identique dans sa forme pour 4 tables différentes | Extraire un composant partagé unique (`src/components/admin/TrashSection.tsx`, props génériques `items`/`isLoading`/`canHardDelete`/`onRestore`/`onHardDelete`) plutôt que de dupliquer le JSX 4 fois — chaque page ne fait que mapper ses propres données vers la forme `{id, label, supprime_le, supprime_par_profile}` attendue.

[2026-08-31] | Migration SQL contenant des textes français longs avec apostrophes multiples (site_content) | Écrire les valeurs JSONB via `jsonb_build_object`/`jsonb_build_array` + `to_jsonb($$...$$::text)` avec des chaînes dollar-quotées (`$$...$$`) plutôt que des littéraux JSON à la main entre guillemets simples SQL — évite tout risque d'erreur d'échappement (apostrophes françaises très fréquentes dans ce contenu) et reste lisible. Les sauts de ligne réels dans le bloc `$$...$$` du fichier SQL sont conservés tels quels dans la valeur stockée (utile pour les champs texte_long multi-paragraphes).

[2026-08-31] | Un composant "éditeur de liste" générique pour un CMS à schéma JSONB variable (ex. historique à 3 champs vs valeurs à 2 champs) | Ne pas coder un éditeur dédié par clé. Déduire les colonnes à afficher depuis `Object.keys(items[0])` du premier élément du tableau chargé (avec un fallback `["titre","texte"]` si la liste est vide) — un seul composant `ListEditor` couvre alors toutes les clés de type "liste" présentes ou futures sans modification.

[2026-08-31] | Un champ CMS texte_long qui remplace plusieurs `<p>` avec des styles différents (ex. un paragraphe en gras au milieu du bloc "Bénévoles" de Club.tsx) | Consolider en un seul champ texte libre (séparateur `\n\n` → un `<p>` par paragraphe au rendu) fait perdre les styles différenciés par paragraphe. C'est un compromis assumé et documenté en tête de migration plutôt qu'un blocage — inventer un mini-système de rich text pour un seul cas serait du sur-ingineering.

[2026-09-01] | Le diagnostic `SELECT DISTINCT` demandé avant migration 018 a révélé "Seniors Féminines"/"Seniors Masculins" SANS accent dans `matches.categorie`, alors que `src/data/categories.ts` ET la fonction SQL `init_match_postes()` (`002_match_postes.sql`) utilisaient déjà "Séniors" AVEC accent de manière cohérente entre eux | Le bug n'était donc pas une incohérence de code (déjà homogène) mais une divergence entre le code et la DONNÉE réelle, invisible sans interroger la base — `init_match_postes()` teste `p_categorie NOT IN ('Séniors Masculins', 'Séniors Féminines')` pour éviter de créer un poste `arbitre`, condition qui n'a jamais matché pour ces 3 matchs faute d'accent → poste `arbitre` créé à tort à chaque fois. Confirme la valeur du principe "avant d'écrire un UPDATE de migration, vérifier la donnée réelle plutôt que supposer que le code fait foi" — ici la question à poser n'était pas seulement "quelles valeurs existent" mais aussi "quelles fonctions/conditions SQL comparent ce champ, et avec quelle orthographe exacte".

[2026-09-01] | Avant d'uniformiser une orthographe dans tout un projet ("liste-moi toutes les occurrences") | Grep sans discrimination remonte du bruit : fichiers de seed dans d'anciennes migrations déjà exécutées (non modifiables rétroactivement, seule une nouvelle migration corrige la donnée live), fichiers non routés dans `App.tsx` donc morts (ici `Teams.tsx`, `Matches.tsx`, `News.tsx`, `Schedule.tsx` — scaffold initial jamais branché), et faux positifs (`tarifs.libelle = "Seniors"` est un texte libre demandé explicitement par l'utilisateur, pas une valeur de la taxonomie catégories). Trier par "affecte une donnée live via une logique de code" vs "cosmétique/mort" avant de proposer des corrections, plutôt que de tout traiter au même niveau de priorité.

[2026-09-01] | Migration UPDATE sur une colonne TEXT[] (`encadrement.categories`) où le mapping n'est pas 1:1 (une valeur source peut converger avec une autre vers la même cible, ET une valeur source doit parfois s'éclater en plusieurs valeurs cibles) | Un `array_replace()` en chaîne ne suffit pas : il peut laisser des doublons (deux valeurs sources convergentes) et ne sait pas éclater une valeur en plusieurs. Pattern robuste : `unnest(...) WITH ORDINALITY` pour garder la position d'origine, `CASE` + second `unnest(...) WITH ORDINALITY` pour mapper/éclater chaque élément vers 0..N valeurs cibles avec leur propre sous-position, `GROUP BY (id, valeur_cible)` avec `MIN(ordinalité)` pour dédupliquer tout en retenant la position de la première occurrence, puis `array_agg(... ORDER BY min_ord, min_sub_ord)` pour reconstruire un tableau trié et sans doublon. Réutilisable pour toute migration de colonne array avec renommage/fusion/scission de valeurs.

[2026-09-01] | Une migration multi-étapes qui s'étale sur plusieurs tours de conversation (l'utilisateur exécute un diagnostic, revient avec le résultat, la migration se complète par étapes) | Garder un bloc "ÉTAT D'AVANCEMENT" (✅/⏳) en tête du fichier SQL, mis à jour à chaque tour, plutôt que de compter sur l'historique de conversation ou les commits Git pour retrouver où on en est — le fichier doit être auto-descriptif si on le rouvre à froid. Chaque diagnostic exécuté est aussi consigné en commentaire (valeurs + comptes) directement dans le fichier, pas seulement dans la réponse du tour concerné.

[2026-09-02] | Réordonnancement `encadrement` (boutons ↑↓) : `handleMove` émettait deux PATCH REST concurrents `{id:a, ordre:b.ordre}` / `{id:b, ordre:a.ordre}` via `Promise.all` — non transactionnel ET sans effet dès qu'il existe un ex-aequo (constaté : 3 entraîneurs à `ordre=11`, on échange 11↔11 = no-op → le bouton semble cassé). En plus les `order=` PostgREST des lectures s'arrêtaient à `ordre.asc` → ordre d'affichage aléatoire entre ex-aequo, donc les ↑↓ portaient sur des positions instables | Pour un "échanger la position de deux lignes" au-dessus de PostgREST : ne PAS faire N PATCH côté client. Créer une fonction SQL (`reorder_encadrement(p_id, p_direction)`) qui, dans **une seule transaction** : (1) densifie la section via une fonction de renumérotation dédiée (`renumber_encadrement(p_type)` = `ROW_NUMBER() OVER (ORDER BY ordre, created_at, id) - 1`), (2) relit l'`ordre` dense de la fiche, (3) échange les deux `ordre` en un `UPDATE ... SET ordre = CASE id ... END WHERE id IN (a,b)`. Appelée en RPC (`POST /rest/v1/rpc/...`), `security invoker` pour rester sous la RLS de la table. Le client ne passe plus que `{id, direction}`. Et TOUJOURS ajouter un critère de tri secondaire stable (`created_at`, `id`) après `ordre` dans les lectures publique + admin — un ex-aequo ne doit jamais produire un ordre non déterministe. Migration 020. Nettoyage des doublons déjà en base = un `UPDATE ... FROM (ROW_NUMBER() OVER (PARTITION BY type ORDER BY ordre, created_at, id))` unique et rejouable.

[2026-09-02] | `Club.tsx` cachait le prénom des membres du bureau sans photo : le prénom était rendu **à l'intérieur** de la branche `person.photo_url ? (...)` — encodage implicite de l'ancienne consigne "seuls Président + Trésorière ont prénom+photo" (cf. leçon 2026-08-31 : `featured` == `avatarUrl renseigné`). Estelle (Secrétaire) et Guillaume (Vice-Président), désormais renseignés en base avec prénom mais sans photo, n'affichaient que leur intitulé | Un rendu alimenté par un CMS ne doit brancher QUE sur la présence de chaque champ pris isolément : `person.prenom &&` pour le prénom, `person.photo_url ?` pour l'image/placeholder, `person.role` toujours. Ne jamais conditionner l'affichage d'un champ à la présence d'un autre, ni à une liste de rôles/noms codée en dur. Vérifié : les 3 sections encadrement de Club.tsx (bureau, pôles, entraîneurs) ne contiennent plus aucune règle en dur — les filtres `type === "..."` restent la seule logique, et c'est légitime (c'est la donnée `type` de la table).

[2026-09-02] | `019_soft_delete.sql` a échoué à la ré-exécution : `ERROR 42701: column "supprime_le" of relation "collectifs" already exists` — `ALTER TABLE ... ADD COLUMN` sans `IF NOT EXISTS`, et seules certaines `CREATE POLICY` avaient un `DROP POLICY IF EXISTS` devant elles (celles qui remplaçaient une policy pré-existante d'avant cette migration ; pas celles créées pour la première fois par cette migration elle-même) | **Règle désormais systématique pour toutes les migrations de ce projet** : `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` partout, et `DROP POLICY IF EXISTS "nom" ON table;` immédiatement avant CHAQUE `CREATE POLICY`, y compris les policies entièrement nouvelles — pas seulement celles qui remplacent quelque chose. Objectif : toute migration doit pouvoir être relancée sans erreur quel que soit l'état d'avancement d'une exécution précédente (partielle ou totale), puisque les migrations de ce projet sont exécutées manuellement par l'utilisateur dans le SQL Editor et peuvent échouer à mi-parcours pour une raison indépendante (ex. erreur de frappe, coupure) sans que Claude puisse le constater directement.
